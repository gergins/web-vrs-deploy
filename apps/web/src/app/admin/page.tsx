export default function AdminPage() {
  return (
    <main>
      <h1>Admin</h1>
      <p>
        Deferred placeholder only. The admin interface is not implemented in the
        current local Web VRS prototype, and runtime workflows do not depend on
        this page yet.
      </p>
    </main>
  );
}
